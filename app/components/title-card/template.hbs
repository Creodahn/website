<section class="{{if fullTitle 'content-section title-card' 'container small-title'}}">
  <header class="name">
    <h1>{{model.fullName}}</h1>
  </header>
  <footer class="title">
    <div>full stack developer</div>
  </footer>
  <button class="btn btn-transparent {{if fullTitle '' 'hidden'}}" {{action "alterTitle"}}>Continue</button>
</section>

{{yield}}
