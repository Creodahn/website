import { equalTo } from 'personal-website/helpers/equal-to';
import { module, test } from 'qunit';

module('Unit | Helper | equal to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equalTo([42]);
  assert.ok(result);
});
