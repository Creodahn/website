import { makeSafe } from 'personal-website/helpers/make-safe';
import { module, test } from 'qunit';

module('Unit | Helper | make safe');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = makeSafe([42]);
  assert.ok(result);
});
